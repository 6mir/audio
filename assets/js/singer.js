document.addEventListener("DOMContentLoaded",async()=>{const articContainer=document.getElementById("artic-card");try{const{artic}=await(await fetch(JsonArtic)).json();const selectedIds=["tomaj","hichkas","fadaei","shervin"];articContainer.innerHTML=selectedIds.map((id)=>artic.find((item)=>item.id===id)).filter(Boolean).map(createCardTemplate).join("")}catch{articContainer.innerHTML=errorMessage}})