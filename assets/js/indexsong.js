document.addEventListener("DOMContentLoaded",async()=>{let t=(await (await fetch(JsonSong).catch(()=>{}))?.json())?.[new URLSearchParams(location.search).get("singer")];document.getElementById("page-title").textContent=t?.title?`آهنگ‌های ${t.title}`:"خواننده مورد نظر یافت نشد",document.getElementById("song-list").innerHTML=t?.songs?t.songs.map(createRemixCardTemplate).join(""):errorMessage});