document.addEventListener("DOMContentLoaded",async ()=>{const song=document.getElementById("song-list");try{const songData=await (await fetch(JsonSong)).json();const selectedSongIds=[1,10,5,9];const filteredSongs=selectedSongIds .map(id=>Object.values(songData).flatMap(artist=>artist.songs).find(song=>song.id===id)) .filter(Boolean);song.innerHTML=filteredSongs.map(createRemixCardTemplate).join("")}catch (error){song.innerHTML=errorMessage}});